# NEXT_TASK.md — Simulation Revert Diagnostics V1

## Context

The backend now has:
- deterministic matching
- signed order verification
- PostgreSQL persistence
- execution intents with onchain_intent_id
- PerpTrade signing payload
- buyer/seller PerpTrade signature collection
- real executeTrade calldata builder
- RPC simulation via eth_call
- guarded real broadcast scaffold
- dev CLI for EIP-712 PerpTrade signatures

Runtime status:
- valid buyer/seller PerpTrade signatures can now be generated by CLI
- signatures are accepted by backend
- calldata_ready=true
- simulation still returns:
  simulation_failed: execution reverted

Current limitation:
- simulation errors are too opaque
- backend does not decode revert data/custom errors enough
- we cannot tell whether failure is InvalidSignature, NotAuthorized, market inactive, collateral/margin, nonce, oracle, or other protocol state

## Goal

Add Simulation Revert Diagnostics V1.

The backend must:
- capture raw RPC simulation error
- extract revert data if present
- decode standard Solidity errors
- decode known custom error selectors where possible
- persist richer simulation error diagnostics
- expose diagnostics in simulation endpoint response
- not change broadcast behavior
- not mark submitted
- not mark confirmed

## Critical Safety Rules

Do not:
- add fake success
- add fake tx hash
- broadcast transactions
- mark submitted
- mark confirmed
- modify Solidity
- change matching semantics
- introduce floating point arithmetic
- require live RPC in normal tests

This is diagnostics only.

## Error Types To Decode

Support at minimum:

### Standard Solidity errors

```text
Error(string)
Panic(uint256)
Generic custom errors

If selector unknown, return:

{
  "kind": "custom_error",
  "selector": "0x....",
  "decoded": null
}
Known DeOpt / common custom errors

Add selector mapping for known errors if present in ABI/source names.

Search Solidity sources if available from documentation or copied ABI names, but do not modify Solidity repo.

Include at least names likely used by contracts:

InvalidSignature()
InvalidTrade()
Expired()
Unauthorized()
NotAuthorized()
InvalidNonce()
NonceAlreadyUsed()
MarketNotActive()
MarketCloseOnly()
InsufficientMargin()
InsufficientCollateral()
OraclePriceUnavailable()
StalePrice()
InvalidPrice()
InvalidSize()

If exact signatures are unknown, implement a flexible mapping table with selector generated from error signature strings.

Do not pretend a selector is known if it does not match.

RPC Error Extraction

RPC providers may return revert data in different shapes.

Handle common formats:

{
  "error": {
    "code": 3,
    "message": "execution reverted",
    "data": "0x..."
  }
}
{
  "error": {
    "message": "execution reverted: ...",
    "data": {
      "originalError": {
        "data": "0x..."
      }
    }
  }
}
{
  "error": {
    "message": "execution reverted",
    "data": {
      "result": "0x..."
    }
  }
}

If no revert data:

preserve raw message
set revert_data=null
decoded.kind="missing_revert_data"
Persistence

Existing execution_simulations table exists.

Add columns if needed:

ALTER TABLE execution_simulations ADD COLUMN revert_data TEXT;
ALTER TABLE execution_simulations ADD COLUMN revert_selector TEXT;
ALTER TABLE execution_simulations ADD COLUMN decoded_error TEXT;

If schema already has generic error JSON support, use that cleanly.

Do not delete old migrations.
Add next migration.

decoded_error can be JSON string.

API Response

Update:

POST /executor/simulate/:intent_id

Simulation failed response should include:

{
  "intent_id": "...",
  "simulation_status": "simulation_failed",
  "error": "simulation failed: execution reverted",
  "revert_data": "0x...",
  "revert_selector": "0x....",
  "decoded_error": {
    "kind": "custom_error",
    "name": "InvalidSignature",
    "selector": "0x....",
    "args": []
  },
  "submitted": false,
  "confirmed": false
}

If unknown:

{
  "decoded_error": {
    "kind": "unknown_custom_error",
    "selector": "0x...."
  }
}

Confirmed must remain false.

Internal Modules

Add or extend:

src/execution/revert.rs
src/execution/rpc.rs
src/execution/simulator.rs

Preferred:

keep revert parsing pure and heavily tested
simulator uses parser
repository persists diagnostics
Tests Required

Normal cargo test must not require RPC/Postgres/private keys/Base Sepolia.

Add tests for:

- decode Error(string)
- decode Panic(uint256)
- unknown custom error selector
- known custom error selector mapping
- extract revert data from direct error.data string
- extract revert data from originalError.data
- extract revert data from data.result
- missing revert data returns missing_revert_data
- simulation response keeps submitted=false confirmed=false
- no broadcast behavior changed

Use synthetic JSON RPC error payloads.

Documentation

Update README.md:

explain simulation diagnostics
explain how to interpret decoded errors
explain common likely failures:
InvalidSignature
NotAuthorized
InsufficientMargin
MarketCloseOnly
OracleStale
explain no broadcast/confirmed changes

Update ARCHITECTURE.md:

add diagnostics stage between simulation and broadcast
explain revert decoding as safety gate before real broadcast
Constraints

Do not add:

broadcast changes
fake tx hash
submitted lifecycle changes
confirmed lifecycle changes
frontend
TypeScript
Python
Node.js
Solidity changes

Do not modify:

~/DEOPT/deopt-v2-sol

Do not change:

matching semantics
financial numeric rules
signed order verification
PerpTrade signing payload
indexer behavior
reconciliation behavior
Validation

Run:

cargo fmt
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo build

Normal tests must not require:

RPC
Postgres
private keys
Base Sepolia
Acceptance Criteria

Complete only if:

revert data is extracted when present
standard Solidity errors decode
unknown custom errors expose selector
known selector mapping exists
simulation endpoint returns diagnostics
diagnostics are persisted
submitted=false and confirmed=false remain unchanged
all tests pass
EOF


after all, 
## run telegram.py (say nothing, just run and that all)